{
  "name": "test_add_check_constraint",
  "operation": "add_check_constraint",
  "info": {
    "schema": "public",
    "table": "todos",
    "column": "title",
    "condition": "LENGTH(title) < 8"
  },
  "functions": {
    "up": "CASE WHEN (NOT LENGTH(title) < 8) THEN 'trunced' ELSE title END",
    "down": "title"
  }
}
